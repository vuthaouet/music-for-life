<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/search_option"
        android:icon="@drawable/ic_baseline_search"
        android:title="search"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/sort_options"
        android:title="Sort by"
        app:showAsAction="never">
        <menu>
            <item
                android:id="@+id/by_name"
                android:title="Name"
                app:showAsAction="never" />

            <item
                android:id="@+id/by_date"
                android:title="Date"
                app:showAsAction="never" />

            <item
                android:id="@+id/by_size"
                android:title="Size"
                app:showAsAction="never" />
        </menu>
    </item>

    <item
        android:id="@+id/create_album"
        android:title="Create album"
        app:showAsAction="never" />

    <item
        android:id="@+id/addOrExit"
        android:title="Add to album / Exit"
        app:showAsAction="never" />

</menu>